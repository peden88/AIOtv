{
  "name": "aiotv-control",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone managed-device pairing and addon control plane for AIOtv",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs",
    "test": "node --test",
    "hash-password": "node scripts/hash-password.mjs"
  }
}

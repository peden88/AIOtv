package com.nuvio.tv.core.aio

object AioTvServerConfig {
    /**
     * Public AIOStreams origin used for Pocket ID QR approval and the AIOtv API.
     * Set this to the Pangolin-exposed AIOStreams origin for the deployment
     * build (for example https://streams.example.com). No trailing slash.
     */
    const val BASE_URL: String = ""
}

name: Build AIOtv Control Server

on:
  push:
    branches: [dev]
    paths:
      - 'control-server/**'
      - '.github/workflows/control-server.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./control-server
          push: true
          tags: |
            ghcr.io/peden88/aiotv-control:dev
            ghcr.io/peden88/aiotv-control:${{ github.sha }}
          platforms: linux/amd64
